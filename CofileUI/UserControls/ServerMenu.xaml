<UserControl x:Class="CofileUI.UserControls.ServerMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CofileUI.UserControls"
			
			 xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
			 xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
			 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
	<Grid x:Name="grid_server" Margin="2,2,2,2" Background="White">
		<!--<ItemsControl ItemsSource="{Binding ServerGroups}">
			<ItemsControl.ItemsPanel>
				<ItemsPanelTemplate>
					<local:ServerGroupRootPanel/>
				</ItemsPanelTemplate>
			</ItemsControl.ItemsPanel>
			<ItemsControl.ItemTemplate>
				<DataTemplate>
					<StackPanel>
						<local:ServerGroupButton Content="{Binding GroupName}"/>
						<local:ServerListBox ItemsSource="{Binding Servers}">
							<local:ServerListBox.ItemTemplate>
								<DataTemplate>
									<local:ServerListBoxItem/>
								</DataTemplate>
							</local:ServerListBox.ItemTemplate>
						</local:ServerListBox>
					</StackPanel>
				</DataTemplate>
			</ItemsControl.ItemTemplate>
		</ItemsControl>-->
	</Grid>
</UserControl>
